what [platformio]
default_envs = esp32dev-4mb

[env]
platform = espressif32
framework = arduino

monitor_speed = 115200

; Bluepad32 is shipped inside a special Arduino-ESP32 core.
; Keep using the upstream main branch archive (current known-good setup).
; NOTE: A prior attempt to pin refs/tags/4.0.2 failed because that tag does not exist in this repo.
platform_packages =
  framework-arduinoespressif32@https://github.com/maxgerhardt/pio-framework-bluepad32/archive/refs/heads/main.zip

; --- FORCE 4MB FLASH BUILD + 4MB PARTITIONS ---
board_build.flash_size = 4MB
board_upload.flash_size = 4MB
board_upload.maximum_size = 4194304
board_build.partitions = default.csv

lib_deps =
  adafruit/Adafruit NeoPixel@^1.12.0

[env:esp32dev-4mb]
board = esp32dev
upload_port = /dev/ttyUSB0
monitor_port = /dev/ttyUSB0
build_flags =
  -DLED_PIN=23

[env:esp32-s3-4mb]
board = esp32-s3-devkitc-1
build_flags =
  -DLED_PIN=1
  -DARDUINO_USB_MODE=1
  -DARDUINO_USB_CDC_ON_BOOT=1
